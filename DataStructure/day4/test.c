#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
	/*printf("请输入需要加密的文章：\n");
	char in[1000];
	gets_s(in);
	printf("加密后的结果为：\n");
	for (int i = 0; i < strlen(in); i++) {
		if (in[i] == 32) {
			puts(number[SIZE - 1]);
			printf("|");
		}
		else {
			puts(number[SIZE - 'A']);
			printf("|");
		}
		if (i % 5 == 0)printf("\n");
	}*/
}